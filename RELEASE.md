# Release Notes

## 2026-02-22
- Removed the video capture stack (configuration, controller, metadata, storage, and export bundle) so the app now only runs ML frame analysis.
- Updated the navigation UI and documentation to drop recording controls and describe the ML-only camera behavior.
